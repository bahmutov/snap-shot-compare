'use strict'


/* eslint-env mocha */
const snapShotCompare = require('.')

describe('snap-shot-compare', () => {
  it('write this test', () => {
    console.assert(snapShotCompare, 'should export something')
  })
})
